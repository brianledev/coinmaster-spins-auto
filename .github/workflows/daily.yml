name: Daily Coin Master Spins Update
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions:
  contents: write           # <-- DIT IS DE FIX

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install tavily-python requests beautifulsoup4

      - name: Run script
        env:
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
        run: python spins_today.py

      - name: Commit & push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add spins_today.json index.html
          git commit -m "Daily update $(date)" || exit 0
          git push
